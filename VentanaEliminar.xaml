<Window x:Class="TPV_Hosteleria.VentanaEliminar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:TPV_Hosteleria"
    mc:Ignorable="d"
    Title="Eliminar" Height="600" Width="700"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    Background="#F4F7F6">

<Window.Resources>
    <SolidColorBrush x:Key="PrimaryColor" Color="#FF6F00"/>
    <SolidColorBrush x:Key="AccentColor" Color="#0078D7"/>
    <SolidColorBrush x:Key="DarkText" Color="#333333"/>
    <SolidColorBrush x:Key="LightText" Color="#777777"/>
    <SolidColorBrush x:Key="CardBackground" Color="White"/>
    <SolidColorBrush x:Key="WarningColor" Color="#D32F2F"/>
    <SolidColorBrush x:Key="SuccessColor" Color="#4CAF50"/>
</Window.Resources>

    <Grid Background="White" Margin="1">
        <Grid.Effect>
            <DropShadowEffect Color="#000000" Direction="0" ShadowDepth="0" BlurRadius="20" Opacity="0.1"/>
        </Grid.Effect>
        
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título ELIMINAR -->
            <TextBlock Grid.Row="0" 
                       Text="ELIMINAR" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Foreground="{StaticResource DarkText}"
                       Margin="0,10,0,30"/>

            <!-- Triángulo de advertencia -->
            <Viewbox Grid.Row="1" 
                     Width="180" 
                     Height="160" 
                     HorizontalAlignment="Center"
                     Margin="0,10,0,30">
                <Canvas Width="200" Height="180">
                    <!-- Triángulo rojo -->
                    <Polygon Points="100,20 20,160 180,160" 
                             Stroke="#D32F2F" 
                             StrokeThickness="6" 
                             Fill="Transparent"/>
                    <!-- Signo de exclamación -->
                    <Rectangle Canvas.Left="94" Canvas.Top="60" 
                               Width="12" Height="60" 
                               Fill="#D32F2F"/>
                    <Ellipse Canvas.Left="94" Canvas.Top="130" 
                             Width="12" Height="12" 
                             Fill="#D32F2F"/>
                </Canvas>
            </Viewbox>

            <!-- Primer texto de confirmación -->
            <TextBlock Grid.Row="2"
                       Text="¿Estás seguro de querer ELIMINAR este elemento?" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center" 
                       TextAlignment="Center"
                       Foreground="{StaticResource DarkText}"
                       TextWrapping="Wrap"
                       MaxWidth="600"
                       Margin="20,0,20,25"/>
            
            <!-- Segundo texto de advertencia -->
            <TextBlock Grid.Row="3"
                       Text="ESTA ACCIÓN NO SE PUEDE DESHACER" 
                       FontSize="16" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center" 
                       TextAlignment="Center"
                       Foreground="{StaticResource DarkText}"
                       TextWrapping="Wrap"
                       MaxWidth="600"
                       Margin="20,0,20,0"/>

            <Grid Grid.Row="4" MinHeight="30"/>

            <!-- Botones SI y NO -->
            <Grid Grid.Row="5" 
                  Margin="0,30,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Botón SI -->
                <Button Grid.Column="0" 
                        x:Name="btnSi"
                        Content="SI" 
                        Height="100"
                        FontSize="48" 
                        FontWeight="Bold"
                        Background="White"
                        Foreground="{StaticResource DarkText}"
                        BorderBrush="{StaticResource DarkText}"
                        BorderThickness="2"
                        Margin="0,0,5,0"
                        Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect Color="#CCCCCC" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource SuccessColor}"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Botón NO -->
                <Button Grid.Column="1" 
                        x:Name="btnNo"
                        Content="NO" 
                        Height="100"
                        FontSize="48" 
                        FontWeight="Bold"
                        Background="White"
                        Foreground="{StaticResource DarkText}"
                        BorderBrush="{StaticResource DarkText}"
                        BorderThickness="2"
                        Margin="5,0,0,0"
                        Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect Color="#CCCCCC" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource WarningColor}"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource WarningColor}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
